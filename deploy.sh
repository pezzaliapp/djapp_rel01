#!/bin/bash
# deploy.sh — NON usare: il CSS viene rigenerato da Vite e può cambiare.
# Il deploy si fa copiando i file compilati manualmente nella root del repo.
# Vedi README.md per le istruzioni.
echo "Usa il metodo manuale descritto nel README."
